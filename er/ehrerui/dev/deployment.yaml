apiVersion: apps/v1
kind: Deployment
metadata:
  name: csi-ehr-er-ui
  namespace: csi-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csi-ehr-er-ui
      tier: frontend
      track: stable
  template:
    metadata:
      labels:
        app: csi-ehr-er-ui
        module: er
        tier: frontend
        track: stable
    spec:
      containers:
        - image: 'harbor.cs.local/vidaplus/ehr-er-ui:4.1.13.0-b37'
          imagePullPolicy: IfNotPresent
          name: csi-ehr-er-ui
          ports:
            - containerPort: 80
              name: http
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 25m
              memory: 256Mi
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: csi-ehr-er-ui-service
  name: csi-ehr-er-ui-service
  namespace: csi-dev
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: csi-ehr-er-ui
  type: ClusterIP

