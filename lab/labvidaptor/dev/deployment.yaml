apiVersion: apps/v1
kind: Deployment
metadata:
  name: csi-net-lab-vidaptor
  namespace: csi-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csi-net-lab-vidaptor
      tier: backend
      track: stable
  template:
    metadata:
      labels:
        app: csi-net-lab-vidaptor
        module: lab
        tier: backend
        track: stable
    spec:
      containers:
        - env:
            - name: ASPNETCORE_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  key: net_environment
                  name: service-configs
            - name: config_server
              valueFrom:
                configMapKeyRef:
                  key: config_server
                  name: service-configs
          image: 'harbor.cs.local/vidaplus/lab-vidaptorintegration:4.0.0.12-a2'
          imagePullPolicy: IfNotPresent
          name: csi-net-lab-vidaptor
          ports:
            - containerPort: 80
              name: http
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 50m
              memory: 256Mi
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: csi-net-lab-vidaptor
  name: csi-net-lab-vidaptor
  namespace: csi-dev
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: csi-net-lab-vidaptor
  type: ClusterIP

